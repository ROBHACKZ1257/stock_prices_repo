export default function reducer(state, action) {

    switch (action.type) {
        case value:
            
            break;
    
        default:
            return (
                state
            )
    }

}